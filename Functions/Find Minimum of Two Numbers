#include <iostream>
using namespace std;

int findMin(int a, int b) {
    if (a < b)
        return a;
    return b;
}

int main() {
    int x, y;
    cin >> x >> y;
    cout << findMin(x, y);
    return 0;
}
