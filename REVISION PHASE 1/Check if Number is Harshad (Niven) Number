int sumDigits(int n) {
    int sum = 0;
    while (n > 0) {
        sum += n % 10;
        n /= 10;
    }
    return sum;
}

bool isHarshad(int n) {
    int s = sumDigits(n);
    return (s != 0 && n % s == 0);
}
